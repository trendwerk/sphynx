{% extends 'base.twig' %}

{% block content %}

	<h1>
		{{title}}
	</h1>

	{{description}}
	
	{% if have_posts %}
		{% for post in posts %}
			{% include 'partials/loop-post.twig' %}
		{% endfor %}

		{{fn( 'tp_pagination' )}}
	{% else %}
		<p>No results found.</p>
	{% endif %}

{% endblock %}
