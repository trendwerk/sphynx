<div class="search-form">

	<form action="{{site.url}}" method="get">
		<input type="text" name="s" placeholder="{{__('Search', 'tp' )}}&hellip;" value="{{fn('get_search_query')}}" />
		<button type="submit">
			<span class="glass"></span>
		</button>
	</form>

</div>
