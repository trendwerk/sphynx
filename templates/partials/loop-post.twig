<article class="{{post.class}}">

	<h2>
		<a href="{{post.permalink}}">
			{{post.title}}
		</a>
	</h2>
	
	{% include 'partials/post-meta.twig' %}

	{% if post.thumbnail %}

		<figure>

			<a href="{{post.permalink}}">
				<img src="{{post.thumbnail.src('thumbnail')}}" alt="{{post.thumbnail.title}}">
			</a>

		</figure>

	{% endif %}
	
	{{post.get_preview( 100, false, __( 'Read more', 'tp' ) )|wpautop}}

</article>
